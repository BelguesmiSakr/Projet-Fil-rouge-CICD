server {
    listen 80;
    
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        
        # Nécessaire pour une Single Page Application (SPA) comme React
        try_files $uri $uri/ /index.html;
    }
}